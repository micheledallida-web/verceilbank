node_modules/

# Generated by `npm run build`, which Vercel runs at deploy time.
# Keep it out of git so a stale copy can never shadow a fresh build.
css/output.css

# Generated by scripts/generate-config.js from SUPABASE_URL /
# SUPABASE_ANON_KEY env vars. Never commit real credentials — see
# js/config.example.js for the expected shape.
js/config.js

# Build artifacts from scripts/bundle-function.js — generate, paste, discard.
dist-functions/
